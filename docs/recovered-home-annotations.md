# Recovered Home annotations

Recovered from Agentation session `mr986uz2-oei76u`.

1. Make the info icon same color as text for all tile states
2. Fade the tiles more because they are still way too visible for the + and - to be clear.
3. Do actually 5 per row, so we wil have just one row right now
4. Reduce the spacing between label and the info icon.
5. When I click on the + or the - I am shortly getting the view over the tile not tinted. Can you improve so that it's always tinted when I am still hovering over the tile? The jump is weird.
6. Update "Add application" to "Log application" everywhere where we use "Add application" (so e.g. all buttons and copy in modals etc.
7. This info copy:
   "We automatically update your weekly application count whenever you log an application with us."
8. Update this so that the + and - toggles end and not go over the label. So they should only cover the rainbow.
9. Update copy to:
   "Log it and let it go. Out of sight, out of mind. It will matter again only when moved to the screening stage."
10. Update copy to:
    "A company moved you forward. Bring the application into your pipeline."
11. Update copy to:
    "SCREENINGS"
12. The + and - are almost there. Can you please stretch the top part of the + and - background so it goes to the top of the tile? Apply to every tile.
13. Update this button (and the button in the Pipeline) to:
    "Move to pipeline"
    Make the button for now same as Add application, so no mustard color.
14. Almost there for the + and -. One thing still, to expand the overlay a bit lower down. Not too much, but right now it ends right at the arc end and we need this to move a bit lower so we have some margin/breathing space there.
15. REduce the space between label and the info icon a bit more please
16. Update copy to: "After you apply, record it here. Out of sight until the screening stage."
17. Update copy to:
    "A company moved you forward. Bring the application into your pipeline."
18. Update for when all goals set for the week are done to:
    "You did it. Take a moment to savor it."
19. Update this copy in all screens to not be italic but normal
20. Break line into two.
    Add a rule that those hover states ( or any states) should never go over the side menu or over edge of the screen. Update that in source files. If does not fit, push the content a bit to the side , but leave always the little triangle arrow centered with the icon it's explaining. The copy itself does not have to be centered if there's no space.
    Update also that hover states should not have more than 40 characters per line. If more, push to next line. Don't ever break words so if needed move to the next line earlier.
21. When disabled, do not show hovers for info icons.
22. Update copy to: "When you don't show up here, we will just automatically add it as a rest day."
